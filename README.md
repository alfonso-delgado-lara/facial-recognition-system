# facial-recognition-system
Facial recognition system in R built from raw RGB images, combining custom PCA, Fisher Discriminant Analysis and kNN with rejection. Includes dimensionality reduction, hyperparameter tuning, threshold calibration, 5-fold validation and external testing on unseen images.
